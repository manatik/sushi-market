import React from 'react'

import { NextPageAuth } from '@common-types/private-route.types'

const RolesPage: NextPageAuth = () => {
	return <div>roles</div>
}

RolesPage.isOnlyRoles = ['admin']

export default RolesPage
